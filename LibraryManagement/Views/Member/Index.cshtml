@{
    ViewData["Title"] = "My Library";

    var activeLoans =
        ViewBag.ActiveLoans
        as IEnumerable<LibraryManagement.Models.BorrowingTransaction>
        ?? [];

    var history =
        ViewBag.History
        as IEnumerable<LibraryManagement.Models.BorrowingTransaction>
        ?? [];

    var availableBooks =
        ViewBag.AvailableBooks
        as IEnumerable<LibraryManagement.Models.Book>
        ?? [];

    var myReservations =
        ViewBag.MyReservations
        as IEnumerable<LibraryManagement.Models.Reservation>
        ?? [];

    decimal unpaidFines = ViewBag.UnpaidFines ?? 0m;
    int overdueCount = ViewBag.OverdueCount ?? 0;
}

<style>
    body {
        background-color: #f5f1e8;
        font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    }

    .dash {
        min-height: 100vh;
        padding: 60px 20px;
        display: flex;
        justify-content: center;
    }

    .dashboard-card {
        width: 100%;
        max-width: 1400px;
        background: #ffffff;
        border: 3px solid #000;
        border-radius: 12px;
        box-shadow: 8px 8px 0 #000;
        padding: 50px;
    }

    /* Header */
    .page-subtitle {
        color: #ed7d3a;
        font-size: 14px;
        font-weight: 700;
        letter-spacing: 2px;
        text-transform: uppercase;
        margin-bottom: 10px;
    }

    .page-heading {
        font-size: 3rem;
        font-weight: 800;
        color: #111;
        margin-bottom: 10px;
    }

    .page-description {
        color: #666;
        font-size: 1.05rem;
        line-height: 1.7;
        margin-bottom: 30px;
    }

    /* Alerts */
    .alert {
        padding: 15px 20px;
        margin-bottom: 20px;
        border: 3px solid #000;
        border-radius: 12px;
        box-shadow: 4px 4px 0 #000;
        font-weight: 600;
    }

    .alert--green {
        background: #e8f7ed;
        color: #1f7a3d;
    }

    .alert--red {
        background: #fdeaea;
        color: #c63c2c;
    }

    /* Stats */
    .stat-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(180px, 1fr));
        gap: 20px;
        margin-bottom: 50px;
    }

    .stat-card {
        background: #faf7f2;
        border: 3px solid #000;
        border-radius: 12px;
        box-shadow: 4px 4px 0 #000;
        padding: 25px;
        text-align: center;
    }

    .stat-card__label {
        font-size: 0.85rem;
        font-weight: 700;
        text-transform: uppercase;
        color: #666;
        margin-bottom: 8px;
    }

    .stat-card__value {
        font-size: 2rem;
        font-weight: 800;
        color: #ed7d3a;
    }

    /* Sections */
    .section {
        margin-bottom: 60px;
    }

    .section__header {
        display: flex;
        justify-content: space-between;
        align-items: center;
        flex-wrap: wrap;
        gap: 15px;
        margin-bottom: 20px;
    }

    .section__title {
        font-size: 1.8rem;
        font-weight: 800;
        color: #111;
        margin: 0;
    }

    /* Buttons */
    .btn {
        display: inline-block;
        padding: 10px 20px;
        border: 3px solid #000;
        border-radius: 12px;
        box-shadow: 4px 4px 0 #000;
        font-weight: 700;
        text-transform: uppercase;
        font-size: 0.8rem;
        text-decoration: none;
        cursor: pointer;
        transition: all 0.2s ease;
    }

        .btn:hover {
            transform: translate(2px, 2px);
            box-shadow: 2px 2px 0 #000;
        }

    .btn--red {
        background: #ed7d3a;
        color: #fff;
    }

    .btn--black {
        background: #111;
        color: #fff;
    }

    .btn--yellow {
        background: #f4c542;
        color: #111;
    }

    /* Loan Cards */
    .loan-list {
        display: flex;
        flex-direction: column;
        gap: 20px;
    }

    .loan-card {
        background: #faf7f2;
        border: 3px solid #000;
        border-radius: 12px;
        box-shadow: 4px 4px 0 #000;
        padding: 25px;
        display: flex;
        justify-content: space-between;
        align-items: center;
        gap: 20px;
        flex-wrap: wrap;
    }

    .loan-card__title {
        font-size: 1.2rem;
        font-weight: 700;
        color: #111;
    }

    .loan-card__author {
        color: #666;
        margin-top: 5px;
    }

    .loan-card__due {
        font-size: 0.9rem;
        font-weight: 600;
        margin-bottom: 10px;
        color: #c63c2c;
    }

    /* Tables */
    .table-box {
        overflow-x: auto;
        background: #fff;
        border: 3px solid #000;
        border-radius: 12px;
        box-shadow: 4px 4px 0 #000;
    }

    table {
        width: 100%;
        border-collapse: collapse;
    }

    thead {
        background: #111;
        color: #fff;
    }

    th,
    td {
        padding: 15px;
        text-align: left;
        border-bottom: 1px solid #e5e5e5;
    }

    tbody tr:hover {
        background: #faf7f2;
    }

    /* Status Badge */
    .status-badge {
        display: inline-block;
        padding: 6px 12px;
        border-radius: 20px;
        font-size: 0.75rem;
        font-weight: 700;
        background: #fff3cd;
        color: #856404;
    }

    /* Book Grid */
    .book-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(230px, 1fr));
        gap: 20px;
    }

    .book-card {
        background: #faf7f2;
        border: 3px solid #000;
        border-radius: 12px;
        box-shadow: 4px 4px 0 #000;
        overflow: hidden;
        display: flex;
        flex-direction: column;
    }

    .book-image {
        width: 100%;
        height: 300px;
        object-fit: cover;
        border-bottom: 3px solid #000;
        background: #f3f3f3;
    }

    .book-image-placeholder {
        width: 100%;
        height: 300px;
        border-bottom: 3px solid #000;
        background: #f3f3f3;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 4rem;
        color: #999;
    }

    .book-content {
        padding: 20px;
        display: flex;
        flex-direction: column;
        flex-grow: 1;
    }

    .book-card__genre {
        font-size: 0.75rem;
        font-weight: 700;
        color: #ed7d3a;
        text-transform: uppercase;
        letter-spacing: 1px;
        margin-bottom: 8px;
    }

    .book-card__title {
        font-size: 1.1rem;
        font-weight: 700;
        color: #111;
        margin-bottom: 6px;
    }

    .book-card__author {
        color: #666;
        margin-bottom: 8px;
    }

    .book-card__library {
        color: #777;
        font-size: 0.9rem;
        margin-bottom: 15px;
    }

    .book-card__actions {
        margin-top: auto;
    }

    /* Empty State */
    .empty-message {
        text-align: center;
        padding: 30px;
        color: #777;
        font-weight: 600;
        background: #faf7f2;
        border: 3px solid #000;
        border-radius: 12px;
        box-shadow: 4px 4px 0 #000;
    }

    /* Responsive */
    @@media (max-width: 768px) {
        .dashboard-card {
            padding: 30px 20px;
        }

        .page-heading {
            font-size: 2.2rem;
        }

        .section__header {
            flex-direction: column;
            align-items: stretch;
        }

        .loan-card {
            flex-direction: column;
            align-items: flex-start;
        }

        .btn {
            width: 100%;
            text-align: center;
        }

        .book-image,
        .book-image-placeholder {
            height: 250px;
        }
    }
</style>

<div class="dash">
    <div class="dashboard-card">

        @if (TempData["Success"] != null)
        {
            <div class="alert alert--green">@TempData["Success"]</div>
        }

        @if (TempData["Error"] != null)
        {
            <div class="alert alert--red">@TempData["Error"]</div>
        }

        <p class="page-subtitle">MEMBER DASHBOARD</p>
        <h1 class="page-heading">My Library</h1>
        <p class="page-description">
            Manage your borrowed books, reservations, reading history and fines.
        </p>

        <!-- Statistics -->
        <div class="stat-grid">
            <div class="stat-card">
                <div class="stat-card__label">Active Loans</div>
                <div class="stat-card__value">@activeLoans.Count()</div>
            </div>

            <div class="stat-card">
                <div class="stat-card__label">Books Read</div>
                <div class="stat-card__value">@history.Count()</div>
            </div>

            <div class="stat-card">
                <div class="stat-card__label">Reservations</div>
                <div class="stat-card__value">@myReservations.Count()</div>
            </div>

            <div class="stat-card">
                <div class="stat-card__label">Overdue</div>
                <div class="stat-card__value">@overdueCount</div>
            </div>

            <div class="stat-card">
                <div class="stat-card__label">Unpaid Fines</div>
                <div class="stat-card__value">$@unpaidFines.ToString("0.00")</div>
            </div>
        </div>

        <!-- Active Loans -->
        <div class="section">
            <div class="section__header">
                <h2 class="section__title">📚 Active Loans</h2>
            </div>

            <div class="loan-list">
                @if (!activeLoans.Any())
                {
                    <div class="empty-message">No active loans.</div>
                }
                else
                {
                    @foreach (var t in activeLoans)
                    {
                        var config = ViewBag.Config as LibraryManagement.Models.BorrowingConfig;
                        int renewalLimit = config?.RenewalLimit ?? 2;
                        int renewalDays = config?.LoanDurationDays ?? 14;
                        int renewalsLeft = renewalLimit - t.RenewalsUsed;
                        bool canRenew = renewalsLeft > 0;
                        bool isOverdue = t.DueDate < DateTime.Now;

                        <div class="loan-card" style="@(isOverdue ? "border-color:#c0392b; background:#fff5f5;" : "")">
                            <div style="flex:1">
                                <div class="loan-card__title">@(t.Book?.Title ?? "—")</div>
                                <div class="loan-card__author">by @(t.Book?.Author ?? "—")</div>
                                <div class="loan-card__due" style="margin-top:8px">
                                    @if (isOverdue)
                                    {
                                        <span style="color:#c0392b">⚠ Overdue since @t.DueDate.ToString("dd MMM yyyy")</span>
                                    }
                                    else
                                    {
                                        <span>Due: @t.DueDate.ToString("dd MMM yyyy")</span>
                                    }
                                </div>
                                <!-- Renewal info -->
                                <div style="margin-top:8px; font-size:0.85rem; color:#666;">
                                    @if (canRenew)
                                    {
                                        <span>
                                            🔄 @renewalsLeft renewal@(renewalsLeft == 1 ? "" : "s") left
                                            — extends by @renewalDays days
                                        </span>
                                    }
                                    else
                                    {
                                        <span style="color:#c0392b">✗ No renewals left (@renewalLimit/@renewalLimit used)</span>
                                    }
                                </div>
                            </div>

                            <!-- Action buttons -->
                            <div style="display:flex; flex-direction:column; gap:10px; min-width:140px">

                                <!-- Renew button -->
                                @if (canRenew)
                                {
                                    <button type="button" class="btn btn--yellow" style="width:100%"
                                            onclick="openModal('renew-modal-@t.Id')">
                                        🔄 Renew
                                    </button>
                                }
                                else
                                {
                                    <button class="btn" disabled
                                            style="width:100%; opacity:0.45; cursor:not-allowed; background:#eee; color:#999">
                                        🔄 No Renewals
                                    </button>
                                }

                                <!-- Return button -->
                                <button type="button" class="btn btn--black" style="width:100%"
                                        onclick="openModal('return-modal-@t.Id')">
                                    Return Book
                                </button>

                            </div>
                        </div>
                    }
                }
            </div>
        </div>

        <!-- My Reservations -->
        <div class="section">
            <div class="section__header">
                <h2 class="section__title">📌 My Reservations</h2>
                <a href="/Member/Browse" class="btn btn--yellow">
                    Browse More
                </a>
            </div>

            <div class="table-box">
                <table>
                    <thead>
                        <tr>
                            <th>Book</th>
                            <th>Reserved At</th>
                            <th>Expiry Date</th>
                            <th>Status</th>
                            <th>Action</th>
                        </tr>
                    </thead>
                    <tbody>
                        @if (!myReservations.Any())
                        {
                            <tr>
                                <td colspan="5">No active reservations.</td>
                            </tr>
                        }
                        else
                        {
                            @foreach (var r in myReservations)
                            {
                                <tr>
                                    <td>@(r.Book?.Title ?? "—")</td>
                                    <td>@r.ReservedAt.ToString("dd MMM yyyy")</td>
                                    <td>@r.ExpiryDate.ToString("dd MMM yyyy")</td>
                                    <td>
                                        <span class="status-badge">@r.Status</span>
                                    </td>
                                    <td>
                                        <button type="button"
                                                class="btn btn--red"
                                                onclick="openModal('cancel-modal-@r.Id')">
                                            Cancel
                                        </button>
                                    </td>
                                </tr>
                            }
                        }
                    </tbody>
                </table>
            </div>
        </div>

        <!-- Recent History -->
        <div class="section">
            <div class="section__header">
                <h2 class="section__title">📖 Recent History</h2>
                <a href="/Member/Browse" class="btn btn--red">
                    Browse Books
                </a>
            </div>

            <div class="table-box">
                <table>
                    <thead>
                        <tr>
                            <th>Book</th>
                            <th>Returned Date</th>
                        </tr>
                    </thead>
                    <tbody>
                        @if (!history.Any())
                        {
                            <tr>
                                <td colspan="2">No borrowing history.</td>
                            </tr>
                        }
                        else
                        {
                            @foreach (var t in history)
                            {
                                <tr>
                                    <td>@(t.Book?.Title ?? "—")</td>
                                    <td>@(t.ReturnedAt?.ToString("dd MMM yyyy") ?? "—")</td>
                                </tr>
                            }
                        }
                    </tbody>
                </table>
            </div>
        </div>

        <!-- Available Books -->
        <div class="section">
            <div class="section__header">
                <h2 class="section__title">📚 Available Books</h2>
            </div>

            @if (!availableBooks.Any())
            {
                <div class="empty-message">
                    No available books right now.
                </div>
            }
            else
            {
                <div class="book-grid">
                    @foreach (var b in availableBooks)
                    {
                        <div class="book-card">

                            @if (!string.IsNullOrEmpty(b.CoverImagePath))
                            {
                                <img src="@b.CoverImagePath"
                                     alt="@b.Title"
                                     class="book-image" />
                            }
                            else
                            {
                                <div class="book-image-placeholder">
                                    📘
                                </div>
                            }

                            <div class="book-content">
                                <div class="book-card__genre">@b.Genre</div>
                                <div class="book-card__title">@b.Title</div>
                                <div class="book-card__author">by @b.Author</div>
                                <div class="book-card__library">
                                    📍 @(b.Library?.Name ?? "Unknown Library")
                                </div>

                                <div class="book-card__actions">
                                    <form asp-controller="Member"
                                          asp-action="Borrow"
                                          method="post">
                                        @Html.AntiForgeryToken()
                                        <input type="hidden"
                                               name="bookId"
                                               value="@b.Id" />
                                        <button type="submit"
                                                class="btn btn--red">
                                            Borrow Book
                                        </button>
                                    </form>
                                </div>
                            </div>

                        </div>
                    }
                </div>
            }
        </div>

    </div>
</div>

<!-- ── Modals ─────────────────────────────────────────── -->
@foreach (var t in activeLoans)
{
    var config2 = ViewBag.Config as LibraryManagement.Models.BorrowingConfig;
    int rl = config2?.RenewalLimit ?? 2;
    int rd = config2?.LoanDurationDays ?? 14;
    int renewalsLeft2 = rl - t.RenewalsUsed;
    bool canRenew2 = renewalsLeft2 > 0;

    <!-- Renew Modal -->
    if (canRenew2)
    {
        <div id="renew-modal-@t.Id" class="site-modal-overlay" style="display:none">
            <div class="site-modal">
                <div class="site-modal__icon">🔄</div>
                <h3 class="site-modal__title">Renew Book</h3>
                <p class="site-modal__body">
                    Renew <strong>@(t.Book?.Title ?? "this book")</strong> for another <strong>@rd days</strong>?
                    <br />
                    <span style="font-size:0.85rem; color:#666">
                        New due date: @t.DueDate.AddDays(rd).ToString("dd MMM yyyy") &nbsp;·&nbsp;
                        @(renewalsLeft2 - 1) renewal@(renewalsLeft2 - 1 == 1 ? "" : "s") remaining after this
                    </span>
                </p>
                <div class="site-modal__actions">
                    <form asp-controller="Member" asp-action="Renew" method="post">
                        @Html.AntiForgeryToken()
                        <input type="hidden" name="id" value="@t.Id" />
                        <button type="submit" class="btn btn--yellow">Yes, Renew</button>
                    </form>
                    <button type="button" class="btn" onclick="closeModal('renew-modal-@t.Id')">Cancel</button>
                </div>
            </div>
        </div>
    }

    <!-- Return Modal -->
    <div id="return-modal-@t.Id" class="site-modal-overlay" style="display:none">
        <div class="site-modal">
            <div class="site-modal__icon">📬</div>
            <h3 class="site-modal__title">Return Book</h3>
            <p class="site-modal__body">
                Return <strong>@(t.Book?.Title ?? "this book")</strong>?
                @if (t.DueDate < DateTime.Now)
                {
                    int overdueDays = (int)(DateTime.Now - t.DueDate).TotalDays;
                    <br />
                    <span style="color:#c0392b; font-size:0.85rem">
                        ⚠ This book is @overdueDays day@(overdueDays == 1 ? "" : "s") overdue. A fine may be applied.
                    </span>
                }
            </p>
            <div class="site-modal__actions">
                <form asp-controller="Member" asp-action="ReturnBook" method="post">
                    @Html.AntiForgeryToken()
                    <input type="hidden" name="id" value="@t.Id" />
                    <button type="submit" class="btn btn--black">Yes, Return</button>
                </form>
                <button type="button" class="btn" onclick="closeModal('return-modal-@t.Id')">Cancel</button>
            </div>
        </div>
    </div>
}

@foreach (var r in myReservations)
{
    <!-- Cancel Reservation Modal -->
    <div id="cancel-modal-@r.Id" class="site-modal-overlay" style="display:none">
        <div class="site-modal">
            <div class="site-modal__icon">📌</div>
            <h3 class="site-modal__title">Cancel Reservation</h3>
            <p class="site-modal__body">
                Cancel your reservation for <strong>@(r.Book?.Title ?? "this book")</strong>?
                <br />
                <span style="font-size:0.85rem; color:#666">This cannot be undone.</span>
            </p>
            <div class="site-modal__actions">
                <form asp-controller="Member" asp-action="CancelReservation"
                      asp-route-id="@r.Id" method="post">
                    @Html.AntiForgeryToken()
                    <button type="submit" class="btn btn--red">Yes, Cancel</button>
                </form>
                <button type="button" class="btn" onclick="closeModal('cancel-modal-@r.Id')">Keep It</button>
            </div>
        </div>
    </div>
}

<!-- Modal CSS -->
<style>
    .site-modal-overlay {
        position: fixed;
        inset: 0;
        background: rgba(0, 0, 0, 0.55);
        z-index: 9999;
        display: flex;
        align-items: center;
        justify-content: center;
        padding: 1rem;
    }

    .site-modal {
        background: #fff;
        border: 3px solid #000;
        border-radius: 16px;
        box-shadow: 8px 8px 0 #000;
        padding: 2.5rem 2rem;
        max-width: 420px;
        width: 100%;
        text-align: center;
        animation: modalIn 0.15s ease;
    }

    @@keyframes modalIn {
        from {
            transform: scale(0.92);
            opacity: 0;
        }

        to {
            transform: scale(1);
            opacity: 1;
        }
    }

    .site-modal__icon {
        font-size: 2.5rem;
        margin-bottom: 0.75rem;
    }

    .site-modal__title {
        font-size: 1.4rem;
        font-weight: 800;
        color: #111;
        margin: 0 0 0.75rem;
    }

    .site-modal__body {
        font-size: 1rem;
        color: #444;
        line-height: 1.6;
        margin: 0 0 1.75rem;
    }

    .site-modal__actions {
        display: flex;
        justify-content: center;
        gap: 1rem;
        flex-wrap: wrap;
    }

        .site-modal__actions .btn {
            min-width: 120px;
        }
</style>

<!-- Modal JS -->
<script>
    function openModal(id) {
        var el = document.getElementById(id);
        if (el) {
            el.style.display = 'flex';
            document.body.style.overflow = 'hidden';
        }
    }

    function closeModal(id) {
        var el = document.getElementById(id);
        if (el) {
            el.style.display = 'none';
            document.body.style.overflow = '';
        }
    }

    // Close modal when clicking outside
    document.addEventListener('click', function (e) {
        if (e.target.classList.contains('site-modal-overlay')) {
            e.target.style.display = 'none';
            document.body.style.overflow = '';
        }
    });
</script>